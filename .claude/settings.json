{
  "hooks": {
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "INPUT=$(cat); [ \"$(echo $INPUT | jq -r '.stop_hook_active')\" = 'true' ] && exit 0; cd $(git rev-parse --show-toplevel) && just test || exit 2"
          }
        ]
      }
    ]
  }
}
